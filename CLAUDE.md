当前仓库是 Analogy Obsidian 插件、公开本地 MCP 和 Runtime 的唯一开发与发布源，不再从旧混合目录同步源码。

本地真实 Obsidian 测试只使用 `npm run build:local`；CI 使用 `npm run build:ci`；`npm run release:prepare` 只准备候选资产，不代表获准发布。创建或推送 Tag、发布 Runtime/GitHub Release、灰度或生产部署前必须取得用户针对本次操作的明确授权。完整规则以本仓库 `AGENTS.md` 为准。商业服务实现只在私有 `obsidian-commercial-service` 仓库维护。

开源的位置：https://github.com/liaocaoxuezhe/obsidian-extension
